spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: mysql
  data:
    jpa:
      repositories:
        enabled: true
logging:
  level:
    root: info
